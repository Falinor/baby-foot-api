machine:
  node:
    version: "8.1.4"

  environment:
    CIRCLE_ENV: test
    NODE_ENV: test
    PATH: "$PATH:$HOME/$CIRCLE_PROJECT_REPONAME/node_modules/.bin"
    CI: true

  dependencies:
    override:
      - yarn

    cache_directories:
      - ~/.cache/yarn

  test:
    override:
      - yarn test
