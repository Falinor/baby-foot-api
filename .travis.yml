language: node_js
node_js:
  - 6
  - 7
  - 8
  - 9

services: mongodb

before_script:
  - sleep 15
  - mongo mydb_test --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'

environment:
  DB_USER: travis
  DB_PASSWORD: test
