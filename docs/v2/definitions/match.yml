type: object
properties:
  _id:
    type: string
  _key:
    type: string
  red:
    type: object
    properties:
      points:
        type: number
        minimum: 0
        maximum: 42
        example: 10
    required:
      - points
  blue:
    type: object
    properties:
      points:
        type: number
        minimum: 0
        maximum: 42
        example: 6
    required:
      - points
  createdAt:
    type: string
    format: date-time
required:
  - red
  - blue
  - createdAt
