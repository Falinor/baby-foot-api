Accept:
  description: The content type wanted for the reponse
  required: true
  schema:
    type: string
    enum:
      - 'application/json'

Content-Type:
  description: The request content type
  required: true
  schema:
    type: string
    enum:
      - 'application/json'
